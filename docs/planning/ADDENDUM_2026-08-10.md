# Addendum — end of session, 10 August 2026

## Patch 19 — shipped and live

Bottom bar (Index · ReVue · Skills · SvPlay, icons only, label on press-and-hold),
tablet navigation restored, EtuDice + iComplete flaps back after the July
unification orphaned them, one name per activity across all four surfaces,
Réglages at `/reglages`, and CI wired in.

`tsc` clean · **742 pages** · `verify19` **21/21** · `verify18` green · applied
twice for idempotency.

**Two of patch 19's eight planned rows did not ship** — the HELP panel deriving
from the registry (1 unit), the foolscap background app-wide (0.75), and the
dead `crumb` prop (0.25). Against that, three things shipped that were never in
the estimate: the gated flaps, the registry rename consolidation, and the CI
scripts. Call it even at ~7 units; **2 units still carry the patch 19 label.**

Worth recording: `verify19` passed 21/21 while the phone was still wrong twice
— the icons-only rail matched nothing (patch 17 had removed the class it keyed
on) and the two draggable floats sat on top of the first and last nav slots.
Both are fixed. **The script proves structure, screenshots prove appearance.**
Neither substitutes for the other, and every future patch should carry both.

## DO FIRST — status

**Done.**

- **Remote topology settled.** `live → dckg/fluo` was configured correctly all
  along; my claim that it wasn't was wrong, and so was "production is stuck at
  patch 12" (it was at 14).
- **Patches 15, 17, 18 applied, committed, pushed to both remotes.**
- **The PII leak is closed and verified on production.** 13 addresses across 8
  learner-facing pages — including the home page, since 25 July — now zero,
  confirmed by curling the deployed chunks, not by inference.

**Still open.**

| | | units |
|---|---|---|
| 🔴 | **Rotate the Firestore service-account key.** Full database access, in `~/Downloads` since 8 Aug. Largest remaining risk, five-minute job. | 0.25 |
| 🟠 | **`/teacher` off the public CDN.** Its own chunk still carries `rosterPrivate.ts`; the sign-in gate is JavaScript that runs after the browser has the file. Needs a Pages Function or a gated deploy — a design decision, not a patch. | 3 |
| 🟡 | **Check `add-claude-github-actions` before merging.** If it adds its own `.github/workflows/*.yml` it will race `verify.yml`. `git diff --stat main origin/add-claude-github-actions` first; keep one, not both. | 0.25 |

## Repo state

```
origin   frenchprof/fluoduo    working repo · ruleset deleted · pushes direct to main
live     dckg/fluo             what Cloudflare Pages builds
                               → fluolingo.com · fluolingo.withdrchan.com · www
backup   dckg/fluonuovo        untouched, 2 Aug, tag pre-duolingo-redesign
```

- `main`, `origin/main` and `live/main` are in sync.
- **`.github/workflows/verify.yml` runs on every push and PR** — tsc, build,
  `verify18.py`, `verify19.py`. Both scripts are checked into `verify/`, out of
  the gitignored patch folders.
- A second workflow, **Preview to GitHub Pages**, predates this and still runs.
  It is not a gate.
- **The ruleset is NOT yet required.** `.github/ruleset-verify.json` is in the
  repo but not applied. After a green run:

  ```
  gh api -X POST repos/frenchprof/fluoduo/rulesets --input .github/ruleset-verify.json
  ```

  Add yourself to its bypass list. A broken CI must never be why a security fix
  waits — that trap cost an hour tonight, twice.
- Commits are authored as `YOUR_NOREPLY@users.noreply.github.com`, a
  placeholder. Worth fixing before a term's history accumulates on it.

## Where the programme stands

**120 units near-term, 8 deferred to December, 12 shipped.** Next is patch 20–21,
the DrillShell — 23 units and the single change that makes the app feel like one
product. Everything downstream of it gets cheaper.

Full detail: `UI_WORK_PLAN.md` (rev 3) · `PATCH_INVENTORY.md` · `UI_AUDIT.md`.
