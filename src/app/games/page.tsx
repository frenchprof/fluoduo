import GamesRedirect from "./GamesRedirect";

/** The old Games hub — retired 2026-09-09, forwards to VocabulaRain. See
 *  GamesRedirect for why. */
export default function GamesPage() {
  return <GamesRedirect />;
}
