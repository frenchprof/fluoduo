import SkillsRedirect from "./SkillsRedirect";

/** The old Skills hub — retired 2026-09-09, forwards to /tts. See
 *  SkillsRedirect for why. */
export default function SkillsPage() {
  return <SkillsRedirect />;
}
